services:
  core:
    build: .
    command: python -m uvicorn manny.main:app --app-dir apps/core --host 0.0.0.0 --port 8765 --no-access-log
    environment:
      MANNY_ENV: development
      MANNY_HARDWARE_MODE: mock
      MANNY_MCP_MODE: mock
    ports:
      - "127.0.0.1:8765:8765"
