# Privacy Model

The runtime exposes the five required privacy states: `PRIVATE_IDLE`, `PRESENT_UNKNOWN`, `PRESENT_TRUSTED`, `MULTIPLE_PEOPLE`, and `PRIVACY_LOCKED`.

The simulator masks its clearly labelled demo finance values for unknown, multiple-person, and locked contexts. This visual behavior demonstrates the policy boundary; authentication is not implemented in Phase 0.
