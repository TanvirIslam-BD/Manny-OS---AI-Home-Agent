[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "manny-os"
version = "0.1.0"
description = "Manny Copilot device runtime and simulator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116,<1",
  "httpx>=0.28,<1",
  "mcp>=2,<3",
  "pydantic>=2.11,<3",
  "pydantic-settings>=2.10,<3",
  "PyYAML>=6.0,<7",
  "tzdata>=2025.2,<2027",
  "uvicorn[standard]>=0.35,<1",
]

[project.optional-dependencies]
dev = [
  "build>=1.2,<2",
  "mypy>=1.17,<2",
  "pip-audit>=2.9,<3",
  "pytest>=9.0.3,<10",
  "pytest-asyncio>=1.1,<2",
  "ruff>=0.12,<1",
  "types-PyYAML>=6.0,<7",
]
production = [
  "keyring>=25,<26",
]
# Only needed when MANNY_PERSON_DETECTOR=opencv_hog. Headless because the device has
# no use for OpenCV's GUI bindings and they pull in more than a kiosk should carry.
vision = [
  "opencv-python-headless>=4.10,<5",
]
# Moonshine and Kokoro are deliberately absent. Both are selectable STT/TTS backends
# (ADR-014), but neither has a version verified against this project on ARM64, and
# pinning one nobody has run would be a guess presented as a requirement — the same
# reasoning that keeps unverified model checksums out of the installer. Install the
# version you validated, then set MANNY_STT_BACKEND / MANNY_TTS_BACKEND. Each adapter
# reports its own absence if the runtime is missing.

[tool.hatch.build.targets.wheel]
packages = ["apps/core/manny"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = [".", "apps/core"]
addopts = "--basetemp=.pytest-tmp -p no:cacheprovider"

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["apps/core", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["manny"]
mypy_path = "apps/core"
plugins = ["pydantic.mypy"]
